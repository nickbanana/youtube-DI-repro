import { WithYtDirective } from './with-yt.directive';

describe('WithYtDirective', () => {
  it('should create an instance', () => {
    const directive = new WithYtDirective();
    expect(directive).toBeTruthy();
  });
});
